name: nfe-rh
regions: fra
services:
  - name: app
    instance_type: nano
    ports:
      - port: 3000
        protocol: http
    env:
      - key: DATA_DIR
        value: /data
    volumes:
      - name: data
        path: /data
    routes:
      - path: /
    health:
      http: /api/config
